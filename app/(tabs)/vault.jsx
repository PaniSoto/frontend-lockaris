import VaultScreen from '@/screens/VaultScreen';

export default function VaultPage() {
  // Expo Router ya maneja el contenedor principal por lo que renderizo directamente la Screen.
  return <VaultScreen />;
}
