import VaultScreen from '@/screens/VaultScreen';

export default function VaultPage() {
  // Expo Router ya maneja el contenedor principal,
  // renderizamos directamente la Screen.
  return <VaultScreen />;
}
